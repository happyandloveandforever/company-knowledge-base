# 公司知识库项目

> **固定协作空间**：以后关于本项目的交流，都在这个 Cursor Agent 会话（本仓库）中进行。

## 项目目标

把公司历年 PPT、Word（后续含书籍）拆成**可复用的结构化知识点**，建立 HTML 总库，按需选题编排，生成 PPT、培训资料、量表等。

## 我们约定的协作方式

| 环节 | 谁来做 | 怎么做 |
|------|--------|--------|
| 上传原始文件 | 你 | 发到对话里，或网页「导入文件」（支持 PDF/HTML/MD/图片等） |
| **精细拆分知识点** | **Claude** | 默认 Claude 精细拆分；无 API Key 时进入待拆分队列，由我在对话中处理 |
| 审核 / 指导修改 | 你 | 在总库看，或告诉我哪条要改、合并、再拆 |
| 选题 + 演讲逻辑 | 一起 | 「编排演讲」页选知识点，或对话里讨论 |
| 导出大纲 / PPT | 系统 + 我 | 网页一键导出，或我按你的要求调整 |

### 拆分方式说明

- **Claude 精细拆分（默认）**：
  - 配置 `ANTHROPIC_API_KEY` → 网页上传后自动 Claude 拆分
  - 未配置 API Key → 文件进入**待拆分队列**，在对话中说「处理拆分队列」
  - 支持格式：PPT · Word · **PDF** · Markdown · HTML · TXT · **PNG/JPG/WebP**（图片用 Claude 视觉）
- **基础机械拆分**：按页/段落拆分，不做 AI 理解
- **Cursor Agent 拆分**：把文件发到对话，我参考 `scripts/import-*.mjs` 逐条精细拆写入库

## 当前进展（2026-08-22）

- [x] 知识库 Web 系统搭建完成（导入 / 总库 / 编排 / PPT 导出）
- [x] 预置 5 条示例知识点
- [x] **漂浮方舟_杨浦区财务.pdf** — Claude 精细拆分为 **55 条**（草稿，待审核）
- [ ] 审核并批准漂浮方舟相关知识点
- [ ] 批量导入更多历史 PPT/Word
- [ ] 定稿公司统一分类体系
- [ ] 培训资料、量表等输出格式

## 系统入口

**推荐（稳定 Preview）：**
```bash
npm run preview
```

开发调试：
```bash
npm run dev -- -p 43123 -H 0.0.0.0
```

- 首页：概览与流程
- `/library` — 知识总库
- `/upload` — 导入文件
- `/compose` — 编排演讲 → 大纲 / PPT

## 数据位置与持久化（重要）

| 路径 | 是否进 Git | 说明 |
|------|-----------|------|
| `data/knowledge-points.json` | ✅ 是 | **核心资产**，所有知识点，必须 commit + push |
| `data/sources.json` | ✅ 是 | 导入文件记录 |
| `data/outlines.json` | ✅ 是 | 大纲历史 |
| `data/analysis-cache.json` | ❌ 否 | 相似/冲突分析缓存，自动生成，删了会重建 |
| `uploads/` | ❌ 否 | 原始 PDF/PPT 备份，**仅本机**；换环境会丢 |

### 大量文件上传时怎么做

1. **知识点**：每次 Claude 拆分或网页导入后，我会 **commit + push** `data/` 到 Git — 这是你的永久库
2. **原始文件**：发在对话里或放 `uploads/`，如需长期保留请自行备份；知识点正文已在 JSON 里
3. **库不会丢**：只要 Git 里有 `knowledge-points.json`，换机器、重启 Preview 都能恢复
4. **性能**：相似/冲突分析有磁盘缓存，只有数据变更后才重算，上千条也不会每次打开都卡

### 当前库位置

就在本仓库根目录：
```
/workspace/data/knowledge-points.json   ← 109 条知识点（会随导入增长）
/workspace/data/sources.json
```

## 冲突与重复处理

| 类型 | 含义 | 怎么处理 |
|------|------|----------|
| **高度重复** | 几乎同一条 | 删掉一条 |
| **内容相似** | 主题相近 | 改标题区分 |
| **内容冲突** | 同一主题说法/数据不同 | 见下方冲突组 |

### 冲突组（`/library/conflicts`）

- 所有冲突版本**并排展示**，一键对比
- **选这个**：设为该组首选（编排演讲时默认用这个）
- **允许冲突·不同客户**：标记为有意为之（如政府版 vs 投资人版愿景），不再当错误
- 编辑时可设：版本标签、版本组 ID、冲突说明

## 下次你可以直接说

- 「上传了 xxx.pptx，帮我 Claude 精细拆」
- 「KP-FAF-048 和 049 合并成一条」
- 「用已批准的知识点，给新人销售做 90 分钟培训，问题-方案-案例逻辑」
- 「导出 HTML 总库」
- 「把医保部分单独做成一个 G 端汇报大纲」

---

**项目名**：公司知识库项目  
**仓库**：本 Git 仓库  
**协作**：Cursor Cloud Agent（Claude）
