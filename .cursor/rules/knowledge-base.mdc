---
description: 公司知识库 — 新对话必须先读 START-HERE.md，禁止 New Project，禁止重建
alwaysApply: true
---

# 公司知识库 — Agent 规则

## ⚠️ 第一件事

1. **读取** `START-HERE.md` 和 `PROJECT.md`
2. **确认** `data/knowledge-points.json` 有 ~109 条数据
3. 若是**空仓库** → **停止**，告诉用户连错了仓库，给出正确地址：
   `https://origin.cursor.com/git/clark-gonzalez/tmp-8ece63bc7e599628`
4. **绝对不要**因空仓库而重建整个项目

## 硬性禁止

- 不要重建 Next.js 项目或换技术栈
- 不要删除或覆盖 `data/knowledge-points.json` 中已有知识点（除非用户明确要求）
- 不要把专利卡写入 `knowledge-points.json`（专利库是 `data/patents.json`）
- 不要重复运行 import 脚本重新生成已入库的文件（如杨浦财务、B端定稿、四簇专利矩阵）。六簇升级用 `scripts/apply-patent-six-modules.mjs`
- 不要清空 `data/sources.json` 中 status=done 的记录

## 交付偏好

- **不要演示视频**（用户 2026-09-04 明确要求）。用文字与测试输出说明结果

## 默认协作模式

- 用户使用 **Cursor 内置 Claude** 做精细拆分，**不需要** `ANTHROPIC_API_KEY`
- 上传 → 拆分队列或对话里拆分 → 写入 JSON → **commit + push** `data/`

## 做完数据变更后

更新 `PROJECT.md` 的「当前库状态」和「变更记录」小节，并 commit。
